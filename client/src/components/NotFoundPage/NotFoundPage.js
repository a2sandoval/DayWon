import React from 'react';

const NotFoundPage = () => (
  <div>
    <h2>Oh no! You can't lift if you're not logged in. Make sure you logged in with the correct credentials. </h2>
  </div>
);

export default NotFoundPage;
